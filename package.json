{
  "name": "portfolio",
  "version": "0.0.1",
  "description": "",
  "keywords": [
    "Portfolio"
  ],
  "license": "",
  "author": "",
  "scripts": {
    "build": "parcel build --no-content-hash index.html",
    "dev": "parcel index.html --open",
    "start": "npm run build && npm run dev",
    "build-css": "node-sass --include-path scss css/main.scss css/main.css",
    "watch-scss": "nodemon -e scss -x \"npm run build-css\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.4",
    "parcel-plugin-static-files-copy": "^2.5.1",
    "sass": "^1.32.5"
  },
  "dependencies": {
    "@parcel/plugin": "^2.0.0-alpha.3",
    "remark-html": "^13.0.1",
    "remark-parse": "^9.0.0",
    "unified": "^9.2.1"
  }
}
