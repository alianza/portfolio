// Variables
const topOffsetBig = 80;
const topOffsetSmall = 48;
const smallWidth = 480;
const mediumWidth = 768;
const largeWidth = 900;

// Elements
const navBar = document.getElementById('navbar');
const dialog = document.getElementById('dialog');
const dialogContent = document.getElementById('dialog-content');
const logo = document.getElementById('logo');

export { topOffsetBig, topOffsetSmall, smallWidth, mediumWidth, largeWidth, navBar, dialog, dialogContent, logo }
